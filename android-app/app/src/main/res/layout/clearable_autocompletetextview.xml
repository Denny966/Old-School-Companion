<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.dennyy.osrscompanion.customviews.DelayedAutoCompleteTextView
            android:id="@+id/delayed_autocomplete_textview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:completionThreshold="9999"
            android:inputType="text"
            android:maxLines="1"
            android:paddingEnd="25dp"
            android:textColor="@color/text"
            android:textColorHint="@color/textSecondary"
            app:autoCompleteDelay="500"/>

        <Button
            android:id="@+id/autocomplete_textview_clear_button"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="5dip"
            android:visibility="gone"
            android:background="@drawable/close_icon"/>

    </RelativeLayout>

</merge>